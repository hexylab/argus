# Issue #61: 映像検索 UI 実装計画

**作成日時**: 2026-01-04
**Issue**: #61 映像検索 UI
**ブランチ**: `feature/#61-video-search-ui`

## 概要

テキストで類似フレームを検索できる UI を実装する。

## 完了条件

- [x] 検索ボックス UI
- [x] 検索結果グリッド表示
- [x] フレーム選択とプレビュー（アノテーションページへ遷移）
- [x] 類似度スコア表示
- [x] ページネーション（Load More ボタン）

## 実装ファイル

### 新規作成

| ファイル | 説明 |
|----------|------|
| `frontend/src/types/search.ts` | 検索関連の型定義 |
| `frontend/src/lib/api/search.ts` | 検索 API クライアント |
| `frontend/src/app/(protected)/projects/[id]/search/page.tsx` | 検索ページ |
| `frontend/src/app/(protected)/projects/[id]/search/components/search-form.tsx` | 検索フォーム |
| `frontend/src/app/(protected)/projects/[id]/search/components/search-results.tsx` | 検索結果グリッド |
| `frontend/src/app/(protected)/projects/[id]/search/search-client.tsx` | クライアントコンポーネント |

## UI 設計

### 検索ページ (`/projects/[id]/search`)

```
┌─────────────────────────────────────────────────────────────┐
│ ダッシュボード > プロジェクト名 > 検索                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  🔍 [テキストで検索...                    ] [検索]           │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  検索結果: 150 件                                            │
│                                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ Thumb    │ │ Thumb    │ │ Thumb    │ │ Thumb    │       │
│  │          │ │          │ │          │ │          │       │
│  ├──────────┤ ├──────────┤ ├──────────┤ ├──────────┤       │
│  │ Frame 42 │ │ Frame 78 │ │ Frame 15 │ │ Frame 99 │       │
│  │ 類似度   │ │ 類似度   │ │ 類似度   │ │ 類似度   │       │
│  │ 0.85     │ │ 0.82     │ │ 0.79     │ │ 0.76     │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│                                                              │
│              [さらに読み込む]                                 │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 実装詳細

### 1. 型定義 (`frontend/src/types/search.ts`)

```typescript
export interface SearchResultItem {
  frame_id: string;
  video_id: string;
  frame_number: number;
  similarity: number;
  s3_key: string;
  thumbnail_url: string | null;
}

export interface SearchResponse {
  results: SearchResultItem[];
  total: number;
  has_more: boolean;
}

export interface SearchRequest {
  query: string;
  video_id?: string;
  limit?: number;
  offset?: number;
  min_similarity?: number;
}
```

### 2. API クライアント (`frontend/src/lib/api/search.ts`)

```typescript
export async function searchFrames(
  accessToken: string,
  projectId: string,
  request: SearchRequest
): Promise<SearchResponse> {
  return apiClient<SearchResponse>(
    `/api/v1/projects/${projectId}/search`,
    {
      method: "POST",
      accessToken,
      body: JSON.stringify(request),
    }
  );
}
```

### 3. 検索ページ構成

- Server Component でプロジェクト情報取得
- Client Component で検索機能実装
- 検索結果は状態管理で保持

## 実装順序

1. [x] 型定義作成
2. [x] API クライアント作成
3. [x] 検索ページ作成
4. [x] プロジェクトページにリンク追加
5. [x] lint/test 実行

## 注意事項

- 類似度スコアは SigLIP 2 の特性上、絶対値は低い（0.01〜0.1 程度）
- 相対的な順序で表示するため、パーセント表示等は避ける
- 検索は Enter キーまたはボタンクリックで実行
