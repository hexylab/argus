# Issue #2: Backend CI ワークフロー (Python)

## 現状分析

既存ファイル:
- `backend/app/__init__.py` - 空のパッケージ
- `backend/tests/__init__.py` - 空のパッケージ

## 実装計画

### 1. pyproject.toml の作成

uv を使用したモダンな Python プロジェクト設定:
- Python 3.12
- 依存関係: FastAPI, uvicorn (後で追加)
- 開発依存: ruff, mypy, pytest, pytest-cov

### 2. ruff.toml の設定

ruff 設定:
- line-length: 88
- target-version: py312
- select: E, F, W, I, UP, B, SIM
- format 設定

### 3. mypy.ini の設定

mypy 設定:
- python_version: 3.12
- strict: true
- plugins: pydantic (後で追加)

### 4. サンプルテストの作成

tests/test_sample.py:
- 基本的なテストケース
- CI が動作することを確認

### 5. GitHub Actions ワークフロー

.github/workflows/backend-ci.yml:
- トリガー: push (main), PR (backend/**)
- ジョブ: lint, typecheck, test
- uv によるセットアップ
- PostgreSQL サービスコンテナ (後で使用)

## ファイル一覧

| ファイル | 操作 | 行数目安 |
|---------|------|---------|
| `backend/pyproject.toml` | 作成 | ~50 |
| `backend/ruff.toml` | 作成 | ~30 |
| `backend/mypy.ini` | 作成 | ~15 |
| `backend/tests/test_sample.py` | 作成 | ~15 |
| `.github/workflows/backend-ci.yml` | 作成 | ~80 |

**合計**: 5ファイル, ~190行

## テスト計画

ローカルで以下を実行して確認:
```bash
cd backend
uv sync
uv run ruff check .
uv run ruff format --check .
uv run mypy .
uv run pytest
```

## 完了条件

- [ ] pyproject.toml 作成
- [ ] ruff.toml 作成
- [ ] mypy.ini 作成
- [ ] test_sample.py 作成
- [ ] backend-ci.yml 作成
- [ ] ローカルテスト実行
- [ ] PR 作成・CI パス確認
