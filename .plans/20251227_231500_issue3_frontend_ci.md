# Issue #3: Frontend CI ワークフロー (TypeScript)

## 現状分析

既存ファイル:
- `frontend/src/.gitkeep` - 空のディレクトリ保持用

## 実装計画

### 1. Next.js 15 プロジェクト初期化

pnpm create next-app を使用せず、手動で最小構成を作成:
- package.json: Next.js 15, React 19, TypeScript
- tsconfig.json: strict mode
- next.config.ts: 基本設定

### 2. ESLint + Prettier 設定

- eslint.config.mjs: flat config 形式
- .prettierrc: フォーマット設定
- .prettierignore: 除外設定

### 3. Vitest 設定

- vitest.config.ts: React テスト設定
- @testing-library/react 使用

### 4. 最小限のアプリ構造

- src/app/layout.tsx: ルートレイアウト
- src/app/page.tsx: ホームページ
- src/__tests__/sample.test.ts: サンプルテスト

### 5. GitHub Actions ワークフロー

.github/workflows/frontend-ci.yml:
- トリガー: push (main), PR (frontend/**)
- ジョブ: lint, typecheck, test
- pnpm によるセットアップ

## ファイル一覧

| ファイル | 操作 | 行数目安 |
|---------|------|---------|
| `frontend/package.json` | 作成 | ~45 |
| `frontend/tsconfig.json` | 作成 | ~30 |
| `frontend/next.config.ts` | 作成 | ~10 |
| `frontend/eslint.config.mjs` | 作成 | ~35 |
| `frontend/.prettierrc` | 作成 | ~10 |
| `frontend/.prettierignore` | 作成 | ~5 |
| `frontend/vitest.config.ts` | 作成 | ~20 |
| `frontend/src/app/layout.tsx` | 作成 | ~20 |
| `frontend/src/app/page.tsx` | 作成 | ~15 |
| `frontend/src/__tests__/sample.test.ts` | 作成 | ~20 |
| `.github/workflows/frontend-ci.yml` | 作成 | ~85 |

**合計**: 11ファイル, ~295行

## テスト計画

ローカルで以下を実行して確認:
```bash
cd frontend
pnpm install
pnpm lint
pnpm format:check
pnpm typecheck
pnpm test
```

## 完了条件

- [ ] package.json 作成
- [ ] tsconfig.json 作成
- [ ] next.config.ts 作成
- [ ] ESLint 設定
- [ ] Prettier 設定
- [ ] Vitest 設定
- [ ] app/layout.tsx 作成
- [ ] app/page.tsx 作成
- [ ] sample.test.ts 作成
- [ ] frontend-ci.yml 作成
- [ ] ローカルテスト実行
- [ ] PR 作成・CI パス確認
