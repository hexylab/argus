# ===========================================
# Docker Compose Environment Variables
# ===========================================
# Copy this file to .env and fill in the values
# cp .env.example .env

# MinIO (S3-compatible storage)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=your-secure-minio-password

# Supabase (get from `make supabase-status` or `npx supabase status`)
# Copy the "Publishable" key and get JWT secret from container
SUPABASE_ANON_KEY=paste-from-supabase-status
SUPABASE_JWT_SECRET=paste-from-supabase-status

# Frontend
FRONTEND_PORT=3000
